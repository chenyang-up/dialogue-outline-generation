# 交接文档只引用净结论

`00_handoff.md` 只引用 / 汇总 `01_conclusions.md` 的**净结论**，不含 `02_raw_log.md` 的流水全文。约束来自下游 agent 的上下文预算：把流水全文塞进去会挤掉它真正需要的信息，因此交接面向净结论而非"过程"。代价是交接文档与流水可能短暂不同步；且**每个归档批次最多一份**交接文档——批次内唯一，不按主题各出一份，同样是为了保住单一入口。
