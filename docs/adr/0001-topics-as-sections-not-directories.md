# 主题留在文件内分节，而非每主题独立目录

整理时切分出的主题是 `01_conclusions.md` 与 `02_raw_log.md` 内部的二级标题，**不产生独立文件或目录**。备选方案是为每个主题建一组 `NN_<主题>/` 子目录，否决它有两层原因：交接文档的用途是给下游 agent 一个单一入口，多份会迫使下游先做选择；而"篇"与"主题"两种粒度在早期文档里已经混用，本就是歧义源头。代价是单个文件可能很长——这一条由 ADR-0003 一并接受。
