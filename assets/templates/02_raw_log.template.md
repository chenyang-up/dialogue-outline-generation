# 问答流水（raw log）

**对话**：<对话标题>
**归档批次**：<YYYYMMDD-HHMMSS>
**技能版本**：<本技能 frontmatter 的 version>

<!--
轮次内容的标记嵌套规则（详见 references/design.md）：
本文件自己占三级标题 —— # 文档标题 / ## 主题 / ### 轮次标记（🧑‍💻 用户 / 🤖 模型）。
所以轮次内容里的标题必须落在 4 级及以下，否则会被当成新的主题或新的轮次，文档结构就错了。

偏移量按本轮实际层级反推（不是固定下沉三级）——设本轮内容里最浅的标题层级为 m，偏移量 = 4 − m：
    m = 1（有 #）      → 偏移 +3：#→####、##→#####、###→######
    m = 2（有 ##）     → 偏移 +2：##→####、###→#####、####→######
    m = 3（有 ###）    → 偏移 +1：###→####、####→#####、#####→######
    m ≥ 4（已是深级）  → 偏移 ≤ 0：删掉多余的 #，把最浅一级提到 ####（#####→####、######→#####）

这样每轮内容的最浅标题都落在 ####，跨轮一致，且把 6 级预算用满 —— 最常见的 1–3 级原文不会塌陷。

- 偏移后超过 6 级的封顶到 ######（Markdown 没有第 7 级）。这是唯一会压平的情形，只在原文用到 4 个及以上层级时发生。
- 围栏代码块（用三重反引号或 ~~~ 包裹）之内原样不动——那里的行首 # 不是标题，改了会损坏代码。
- 围栏之外、处于行首的 --- 与 === 转义为 \--- 与 \===。否则它紧跟正文行时会触发 setext 标题，
  把上面那行正文变成二级标题，主题结构同样被破。
- 其余标记（粗体、列表、表格、行内代码、链接等）原样保留。
- 除"标题层级偏移"与"行首分隔线转义"这两处外，文字与标记逐字保留。
-->

## 主题 1：<主题名>

### 🧑‍💻 用户
<原始提问>

### 🤖 模型
<原始回答。原文里若带标题，按本轮偏移量落级 —— 本例本轮最浅标题是「## 结论」，故偏移 +2：>

#### 结论（原文为 ##）

##### 依据（原文为 ###）

```bash
# 围栏内的行首 # 是注释、不是标题 —— 原样保留、不参与偏移
echo "---"
```

---

## 主题 2：<主题名>
<!-- 主题是文件内的二级标题，不产生独立文件。
     仅当两个主题之间明确无逻辑关联时才切分。
     内容全部是技能痕迹的主题整节消失，后续主题编号重排、不留空号。 -->

### 🧑‍💻 用户
<下一轮提问>

### 🤖 模型
<下一轮回答>

---

> 本文件保留对话正文的**文字**（含代码块）；轮次内容中的标题已按**本轮偏移量**落级、行首分隔线已转义，
> 因此文中出现的标题层级与原文不同，这是约定而非原文如此。**框架噪音**（系统提醒、注入块、模型内部推理等）
> 与**技能痕迹**（本技能自身的触发、执行、回执与交互回答）已剔除；承载结论的工具结果以摘要形式保留。
> 供备份与回溯之用，提炼后的净结论见 `01_conclusions.md`。
